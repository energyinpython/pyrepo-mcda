:py:mod:`pyrepo_mcda.normalizations`
====================================

.. py:module:: pyrepo_mcda.normalizations


Module Contents
---------------


Functions
~~~~~~~~~

.. autoapisummary::

   pyrepo_mcda.normalizations.linear_normalization
   pyrepo_mcda.normalizations.minmax_normalization
   pyrepo_mcda.normalizations.max_normalization
   pyrepo_mcda.normalizations.sum_normalization
   pyrepo_mcda.normalizations.vector_normalization
   pyrepo_mcda.normalizations.multimoora_normalization



.. py:function:: linear_normalization(matrix, types)

   Normalize decision matrix using linear normalization method.

   :param matrix: Decision matrix with m alternatives in rows and n criteria in columns
   :type matrix: ndarray
   :param types: Criteria types. Profit criteria are represented by 1 and cost by -1.
   :type types: ndarray

   :returns: Normalized decision matrix
   :rtype: ndarray


.. py:function:: minmax_normalization(matrix, types)

   Normalize decision matrix using minimum-maximum normalization method.

   :param matrix: Decision matrix with m alternatives in rows and n criteria in columns
   :type matrix: ndarray
   :param types: Criteria types. Profit criteria are represented by 1 and cost by -1.
   :type types: ndarray

   :returns: Normalized decision matrix
   :rtype: ndarray


.. py:function:: max_normalization(matrix, types)

   Normalize decision matrix using maximum normalization method.

   :param matrix: Decision matrix with m alternatives in rows and n criteria in columns
   :type matrix: ndarray
   :param types: Criteria types. Profit criteria are represented by 1 and cost by -1.
   :type types: ndarray

   :returns: Normalized decision matrix
   :rtype: ndarray


.. py:function:: sum_normalization(matrix, types)

   Normalize decision matrix using sum normalization method.

   :param matrix: Decision matrix with m alternatives in rows and n criteria in columns
   :type matrix: ndarray
   :param types: Criteria types. Profit criteria are represented by 1 and cost by -1.
   :type types: ndarray

   :returns: Normalized decision matrix
   :rtype: ndarray


.. py:function:: vector_normalization(matrix, types)

   Normalize decision matrix using vector normalization method.

   :param matrix: Decision matrix with m alternatives in rows and n criteria in columns
   :type matrix: ndarray
   :param types: Criteria types. Profit criteria are represented by 1 and cost by -1.
   :type types: ndarray

   :returns: Normalized decision matrix
   :rtype: ndarray


.. py:function:: multimoora_normalization(matrix, types)


